rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Solo usuarios autenticados pueden subir y descargar PDFs.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
